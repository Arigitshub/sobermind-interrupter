{
  "name": "sobermind-interrupter",
  "version": "1.0.0",
  "description": "SoberMind Autopilot Interrupter - Premium cognitive delay dashboard.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "cross-env DEPLOY_TARGET=gh-pages npm run build && gh-pages -d dist",
    "deploy:surge": "npm run build && surge dist sobermind-interrupter.surge.sh"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "gh-pages": "^6.0.0",
    "surge": "^0.27.4",
    "vite": "^5.0.0"
  }
}
